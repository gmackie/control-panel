import React from 'react'
import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/card'

export function RollbackManager() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Rollback Manager (stub)</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is a placeholder RollbackManager component.</p>
      </CardContent>
    </Card>
  )
}

